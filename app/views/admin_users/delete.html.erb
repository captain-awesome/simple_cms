<% @page_title = "Delete Admin User" %>

<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="user delete">
  <h2>Delete Admin User</h2>

  <%= form_for(:admin, :url => {:action => 'destroy', :id => @admin_user.id}) do |f| %>

    <p>Are you sure you want to permanently delete this admin?</p>

    <p class="reference-name"><%= @admin_user.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Admin User") %>
    </div>

  <% end %>
</div>
